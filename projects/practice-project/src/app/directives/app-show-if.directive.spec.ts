import { AppShowIfDirective } from './app-show-if.directive';

describe('AppShowIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShowIfDirective();
    expect(directive).toBeTruthy();
  });
});
